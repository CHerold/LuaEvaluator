--#class "example"
--#description "In case of a compile error, no imports will execute."
--#import "imports/importa.lua"
-- On the Mac, add a leading "/" or ":" to the path.
--
--Though the import scripts execute before the main script,
--all scripts are compiled before any execution commences so
--that a compile error in the main script will preempt any
--execution, even of import scripts.
end do while for
